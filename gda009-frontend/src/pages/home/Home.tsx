import { Typography } from "@mui/material";

const Home: React.FC = () => (
  <Typography variant="h1" component="div" sx={{ mt: 3, flexGrow: 1 }}>
    PRUEBA HOME
  </Typography>
);

export default Home;
